# Base mob components that originate from ES.
- type: entity
  id: ESBaseMob
  abstract: true
  components:
  # F14: ESInherentLight component was never ported, so the entry did nothing.
  - type: ESViewcone
  - type: ESViewconeOccludable
  - type: MouseRotator
  - type: NoRotateOnMove
